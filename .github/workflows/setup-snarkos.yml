name: Setup snarkOS

on:
  workflow_call:

jobs:
  setup:
    runs-on: ubuntu-latest-m
    steps:
      - name: Checkout snarkOS repository
        uses: actions/checkout@v5
        with:
          repository: provableHQ/snarkOS
          path: snarkOS
          fetch-depth: 1
          token: ${{ secrets.GITHUB_TOKEN }}

      - name: Set up Rust
        uses: actions-rust-lang/setup-rust-toolchain@v1
        with:
          toolchain: stable
          cache: false

      - name: Set up Rust cache
        uses: Swatinem/rust-cache@v2
        with:
          workspaces: snarkOS
          shared-key: snarkos-cache
          save-if: true

      - name: Install snarkOS dependencies
        env:
          RUSTFLAGS: "-C target-cpu=native"
          CARGO_INCREMENTAL: 0
        run: |
          cd snarkOS
          cargo build --release --features=test_network

      # Upload only the binary (not full repo with target directory)
      # This reduces artifact size from ~3.3GB to ~111MB
      - name: Upload snarkOS binary
        uses: actions/upload-artifact@v5
        with:
          name: snarkos-build
          path: snarkOS/target/release/snarkos
          retention-days: 1

